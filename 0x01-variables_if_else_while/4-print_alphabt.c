#include <stdio.h>
/**
 * main - entry point
 *
 * Return: Always return 0
 */
int main(void)
{
char a;
for (a = 'a'; a <= 'z'; a++)
{
if (a == 'q' || a == 'a')
{
continue;
}
putchar (0);
return (0);
}
